syntax = "proto3";

package hud;

service HudService {
  rpc StreamHud(HudRequest) returns (stream HudFrame);
}

message HudRequest{
  uint32 target_fps =1;
}
message HudFrame {
  bytes jpeg =1;
  uint64 ts =2;
}